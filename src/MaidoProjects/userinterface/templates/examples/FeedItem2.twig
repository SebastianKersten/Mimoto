{% block content %}
<div class="example_feeditem2" {{ Aimless.live()|raw }} onClick="window.open('/article/{{ Aimless.meta('id') }}', '_self')">
    <b>Explainer - <span {{ Aimless.live('title')|raw }}>{{ Aimless.data('title') }}</span></b><br>
    <span {{ Aimless.live('lede')|raw }}>{{ Aimless.data('lede') }}</span>
</div>
{% endblock %}
