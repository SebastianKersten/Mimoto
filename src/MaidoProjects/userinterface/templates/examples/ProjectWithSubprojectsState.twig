{% extends "examplebase.twig" %}

{% block content %}
    <h1>Example 6 - Project with subprojects (typed)</h1>
    <i>This project contains a collection of subprojects and every subproject is presented with the a template based on the "state" property</i><br>
    <br>
    <br>
    <div class="example_project" {{ Aimless.realtime()|raw }}>
        <b><span {{ Aimless.realtime('name')|raw }}>{{ Aimless.data('name') }}</span></b><br><br>
        <div {{ Aimless.realtime('description')|raw }}>{{ Aimless.data('description') }}</div>
        <div class="example_project_subprojects" {{ Aimless.realtime('subprojects')|raw }}>
            {{ Aimless.data('subprojects') }}
        </div>
    </div>
{% endblock %}
