{% block content %}
<div class="example_feeditem1" {{ Aimless.live()|raw }}>
    <b><span {{ Aimless.live('title')|raw }}>{{ Aimless.data('title') }}</span></b><br>
    <span {{ Aimless.live('lede')|raw }}>{{ Aimless.data('lede') }}</span>
</div>
{% endblock %}
