{%
    include "components/data/simplelist_item.twig"
    with 
    {
        id                  : agency.getId, 
        label               : agency.getName,
        
        entityType          : agency.getEntityType,
        
        mls_id              : agency.getAimlessId(), 
        mls_value__label    : agency.getAimlessValue('name'),
        mls_template        : 'AgencyListItem'
    }
%}