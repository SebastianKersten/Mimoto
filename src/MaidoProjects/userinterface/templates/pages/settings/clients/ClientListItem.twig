{%
    include "components/data/simplelist_item.twig"
    with 
    {
        id                  : data.getId(), 
        label               : data.getValue('name'),
        
        entityType          : data.getEntityType(),
        
        mls_id              : data.getAimlessId(), 
        mls_value__label    : data.getAimlessValue('name'),
        mls_template        : 'ClientListItem'
    }
%}