{%
    include "components/data/simplelist_item.twig"
    with 
    {
        id                  : client.getId, 
        label               : client.getName,
        
        entityType          : client.getEntityType,
        
        mls_id              : client.getAimlessId(), 
        mls_value__label    : client.getAimlessValue('name'),
        mls_template        : 'ClientListItem'
    }
%}