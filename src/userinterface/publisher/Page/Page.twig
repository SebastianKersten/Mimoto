{% extends "publisher/base.twig" %}

{# --- page ---#}

{% block content %}


    <button {{ Mimoto.add('elements', [{label:'Component: Element 1'}, 'element2'], { label:'Select a component'}) }}>Add element</button>

    <div {{ Mimoto.realtime('elements', 'element', { values:{ xxx:'yyy' } }) }}>
        {{ Mimoto.data('elements', 'element', { values:{ xxx:'yyy' } }) }}
    </div>

{% endblock %}

