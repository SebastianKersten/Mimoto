{% extends "MimotoCMS/components/base.twig" %}

{% block content %}

    {% set extends = Aimless.data('extends.name') %}
    {# set extends_id = Aimless.meta('extends.id') #}{# todo #}
    {% set extends_id = Aimless.meta('id') %}{# todo #}


    {%
        embed "MimotoCMS/components/modules/Tabmenu/Tabmenu.twig" with {
            id: 'entitydetails',
            tabs: [
                {
                    label: 'Structure',
                    panel_id: 'panel_entitydetail_structure'
                },
                {
                    label: 'Properties',
                    panel_id: 'panel_entitydetail_properties'
                },
                {
                    label: 'More',
                    panel_id: 'panel_entitydetail_more'
                }
            ]
        }
    %}
        {% block panels %}

            {% include "MimotoCMS/components/pages/entities/EntityDetail/Structure/Structure.twig" with { sPanelName:"", Aimless:Aimless }%}
            {% include "MimotoCMS/components/pages/entities/EntityDetail/Properties/Properties.twig" with { sPanelName:"panel_entitydetail_properties", Aimless:Aimless }%}
            {% include "MimotoCMS/components/pages/entities/EntityDetail/More/More.twig" with { sPanelName:"panel_entitydetail_more", Aimless:Aimless }%}

        {% endblock %}
    {% endembed %}

    // 1. Aimless.render('properties', 'property_detail')<br>
    // 2. Abstract properties tonen<br>
    <br>
    <br>
    Aimless.render('properties', 'property_detail')<br>
    Aimless.data('properties')<br>
    <br>
    Only get real data? how about connection id's Aimless.connection()<br>
    <br>


{% endblock %}
