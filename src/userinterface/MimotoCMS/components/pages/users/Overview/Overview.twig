{{ Mimoto.module('MimotoCMS_modules_Button', { label:'Add new user', icon:'plus', directive:Mimoto.add('users', '_Mimoto_user'), types:['secondary'] }) }}

<div class="MimotoCMS_pages_users_Overview-container" {{ Mimoto.sortable() }} {{ Mimoto.realtime('users', 'MimotoCMS_users_Overview_UserPreview') }}>
    {{ Mimoto.data('users', 'MimotoCMS_users_Overview_UserPreview') }}
</div>

<div class="{{ Mimoto.init()  }}" {{ Mimoto.showWhenEmpty('users') }}>
    More users is more fun! Invite more people to your project :)
</div>
