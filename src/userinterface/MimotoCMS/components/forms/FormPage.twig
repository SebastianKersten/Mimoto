{% extends "MimotoCMS/components/base.twig" %}

{% block content %}
    <div class="form_holder">
        <div class="MimotoCMS_forms_Form__fields">
            {{ Aimless.form() }}
        </div>
        <div class="MimotoCMS_forms_Form__menu">
            <span {{ Aimless.submit() }}>
                {{ Aimless.module('Mimoto.CMS.ButtonModule', { label:"Save", onclick:"Mimoto.form.submit();" }) }}
            </span>
        </div>
    </div>
{% endblock %}
