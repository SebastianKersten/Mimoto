{% embed "MimotoCMS/components/forms/form_component.twig" with {"type": "radio-button"} %}
  {% block form_component_element %}

    {% set aOptions = Aimless.fieldOptions() %}
    {% for option in aOptions %}
      <div class="MimotoCMS-forms-FormComponent-element-container">
        <input {{ Aimless.input()|raw }} type="radio" value="{{ option.key }}" class="MimotoCMS_forms_input_RadioButton js-radio-button" {% if Aimless.value() == option.key %}checked{% endif %}/>
        <label for="yes" class="MimotoCMS-forms-FormComponent-element-label">{{ option.value }}</label>
      </div>
    {% endfor %}

  {% endblock %}
{% endembed %}
