{% extends "examples/components/examplebase.twig" %}

{% block content %}
    <h1>Example 1 - Article</h1>
    <i>An article presented in the primary article layout</i><br>
    <br>
    <br>
    <div class="example_article" {{ Aimless.realtime() }}>
        <i><span {{ Aimless.realtime('lede') }}>{{ Aimless.render('lede') }}</span></i><br><br>
        <b><span {{ Aimless.realtime('title') }}>{{ Aimless.render('title') }}</span></b><br><br>
        <div {{ Aimless.realtime('body') }}>{{ Aimless.render('body') }}</div>
    </div>
{% endblock %}
