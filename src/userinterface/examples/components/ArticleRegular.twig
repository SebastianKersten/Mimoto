{% extends "examples/components/examplebase.twig" %}

{% block content %}
    <h1>Example 2 - Article type</h1>
    <i>An article presented in an article layout based on the article type (either 'regular' or 'explainer')</i><br>
    <br>
    <br>
    <div class="example_article_regular" {{ Aimless.realtime() }}>
        <i><span {{ Aimless.realtime('lede') }}>{{ Aimless.data('lede') }}</span></i><br><br>
        <b><span {{ Aimless.realtime('title') }}>{{ Aimless.data('title') }}</span></b><br><br>
        <div {{ Aimless.realtime('body') }}>{{ Aimless.data('body') }}</div>
    </div>
{% endblock %}
